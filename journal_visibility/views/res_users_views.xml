<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <!-- Inherit Form View to Modify it -->
    <record id="allowed_journal_view_users_form" model="ir.ui.view">
        <field name="name">allowed.journal.view.users.form</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">

            <xpath expr="//notebook" position="inside">
                <page string="Allowed Journals">
                    <group>
                        <field name="allowed_journal_ids" widget="many2many_tags" options="{'no_create': True}"/>
                    </group>
                </page>
            </xpath>

        </field>
    </record>

</odoo>